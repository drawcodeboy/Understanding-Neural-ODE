expr: Test ResNet (One Block) (MNIST)

device: &device cuda:0
parallel: false

data:
  dataset: MNIST
  root: data/
  download: True
  mode: test

model:
  name: ResNet
  layers: 1

hyperparameters:
  batch_size: 1000

save:
  weights_path: saved/weights
  weights_filename: one_resnet_mnist.epochs_159.pth