expr: Test RK-Net (MNIST)

device: &device cuda:0
parallel: false

data:
  dataset: MNIST
  root: data/
  download: True
  mode: test

model:
  name: OfficialODENet
  adjoint: false
  solver: dopri5

hyperparameters:
  batch_size: 1000

save:
  weights_path: saved/weights
  weights_filename: rknet_mnist.epochs_160.pth