expr: Test ODE-Net (Adams with adam) (MNIST)

device: &device cuda:0
parallel: false

data:
  dataset: MNIST
  root: data/
  download: True
  mode: test

model:
  name: OfficialODENet
  adjoint: true
  solver: implicit_adams

hyperparameters:
  batch_size: 1000

save:
  weights_path: saved/weights
  weights_filename: odenet_adams_adam_mnist.epochs_083.pth